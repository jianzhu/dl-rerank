{
    "item.goods_ids": {
        "default": 0,
        "type": "sequence_categorical",
        "vocab": "goods_id.txt",
        "vocab_size": 1000000
    },

    "item.shop_ids": {
        "default": 0,
        "type": "sequence_categorical",
        "vocab": "shop_id.txt",
        "vocab_size": 10000
    },

    "item.cate_ids": {
        "default": 0,
        "type": "sequence_categorical",
        "vocab": "cate_id.txt",
        "vocab_size": 1000
    },

    "item.goods_prices": {
        "default": 0,
        "type": "sequence_categorical",
        "vocab": "goods_price.txt",
        "vocab_size": 100
    },

    "item.show_pos": {
        "default": 0,
        "type": "sequence_categorical",
        "vocab": "show_position.txt",
        "vocab_size": 100
    },

    "item.rank_pos": {
        "default": 0,
        "type": "sequence_categorical",
        "vocab": "rank_position.txt",
        "vocab_size": 30
    }
}
